object Room {

    object Version {
        const val version = "2.5.1"
    }

    const val runtime = "androidx.room:room-runtime:${Version.version}"

    const val ktx = "androidx.room:room-ktx:${Version.version}"

    const val compiler = "androidx.room:room-compiler:${Version.version}"

}